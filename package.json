{
  "name": "pms-hotel",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix Backend/pms-backend install",
    "build": "npm --prefix Backend/pms-backend run build",
    "start": "npm --prefix Backend/pms-backend run prisma:deploy && npm --prefix Backend/pms-backend start",
    "dev": "npm --prefix Backend/pms-backend run dev"
  }
}
