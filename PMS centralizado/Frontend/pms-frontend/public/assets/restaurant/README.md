Place restaurant POS icons here (PNG recommended).

- `table-occupied.png` → shown on TPV floorplan when a table has an open order.
- `camastro-free.png` → shown for tables with kind `camastro` when free.
- `bar.svg` → decorative bar icon for floorplan objects.
